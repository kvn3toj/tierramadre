# Audit · commits from 2026-05-25 → 2026-05-26

**Scope:** 32 commits on `main` between 2026-05-25 00:00 and 2026-05-26 04:00 -05.
**Author:** Kvn3Toj (with Claude Opus 4.7 co-author on most).
**App version at audit time:** `2026.05.26.264` (matches latest commit `f37dc3e`).

## Verification summary

| Check | Result |
|---|---|
| `tsc --noEmit` (frontend `tsconfig.json`) | ✅ clean |
| `tsc --noEmit -p api/tsconfig.json` | ⚠️ 2 pre-existing errors in `api/invitations.ts` — unrelated to this window (originated 2026-04-13) |
| `vitest run` (full suite) | ✅ 133 tests pass across 17 files |
| `vite build` | ✅ 14,666 modules, only the standard 800 kB chunk warning |
| Uncommitted working-tree changes | ⚠️ 4 files modified + 1 new test (`tests/itemDrafts.test.ts`) — see “Open work” below |

Every commit message in the window accurately describes the change found in the
code. No misleading commits. No drift between commit description and
implementation.

## Per-commit verdict

### Fotosíntesis — capture flow (11)

| Commit | Claim | Verdict |
|---|---|---|
| `f37dc3e` | Auto-fill the lone item from the lot header on single-item lots | ✅ `CapturaLotePage.tsx:1349` `isSingleItemLot`, prefill effect seeds gema/bruto/joya drafts once per `loteId`. Backed by `seedGemaFromLot`/`seedBrutoFromLot`/`seedJoyaFromLot` in `JoyaFields/GemaFields/BrutoFields`, plus `PricePerCaratHint` and `tests/caratWeight.test.ts` (8 tests). |
| `8aa93cf` | Replace inline ghost-text with dropdown `SuggestInput` | ✅ `InlineSuggestInput.tsx` deleted; new `SuggestInput.tsx` (190 lines) used by 5 form modules. |
| `6a9f6a9` | Inline ghost-text suggestions replace invisible `<datalist>` | ✅ Superseded by `8aa93cf`; the intermediate ghost-text component is no longer present (as intended). |
| `d02e930` | Allow editing item details in any lot estado | ✅ `convex/lotItems.ts:281` and `:405` drop the `abierto`-only guard; `EditItemDrawer` no longer disables based on estado. |
| `73896c3` | Default Procedencia to Boyacá | ✅ `data/vocabularies.ts` + initial values in `BrutoFields.tsx:2`, `GemaFields.tsx:2`. |
| `9e42716` | Sticky base data + remembered write-ins | ✅ `constants/storage-keys.ts` adds 14 keys; `utils/customVocabularies.ts` (119 lines) persists write-ins; `SelectField.tsx` picks them up. |
| `36f0f0d` | Show item names in capture bandeja | ✅ `convex/lotItems.ts` exposes `nombre` on `listByLote`; `CapturaLotePage.tsx` renders it in the bandeja list. |
| `08b69cd` | Edit item photos after a lot is closed | ✅ `convex/lotItems.ts` adds `updateItemPhotos`/`uploadItemPhotoUrl`; `EditItemDrawer.tsx` mounts a `PhotoDropzone` slot for closed lots. |
| `c8875ec` | Item-type selector with 9 sentence-case subtypes | ✅ `CapturaLotePage.tsx:179`-block adds the segmented selector with 9 subtypes. |
| `dda6463` | Spanish spell-check + right-click correction | ✅ `utils/fieldLang.ts` exports `spanishText`/`spanishProperNoun`/`spanishCode` presets; spread onto 11 forms; `utils/pwa.ts` allows the context menu on inputs. |
| `1c148ff` | Add “Otra opción” write-in to all capture dropdowns | ✅ `SelectField.tsx` (221 lines) implements the write-in; rolled out across `JoyaFields`, `ClienteFinalForm`, `EditLotDrawer`, `ProveedorNuevoDrawer`; `SegmentedControl.tsx` extended too. |

### Fotosíntesis — ledger, layout, thumbnails (5)

| Commit | Claim | Verdict |
|---|---|---|
| `55ac61e` | All-lotes ledger page + “Ver todos” from Home | ✅ New `LotesPage.tsx` (768 lines) with estado tabs + debounced search; route `/admin/fotosintesis/lots` wired in `App.tsx:560`; “Ver todos” link in `HomePage.tsx:612-625`. |
| `5a634ef` | Responsive layout + viewport pass across admin module | ✅ 33 files updated; mobile breakpoints normalized across `HomePage`, `LoteResumenPage`, `VentaPage`, `CapturaLotePage`, and ~25 components. |
| `f044c17` | Desktop-first layout + breakpoint consistency pass | ✅ Largest layout refactor — `LoteResumenPage` (831 lines reshaped), `FotoTopbar`, `SegmentedControl`; complements `5a634ef`. |
| `1aed01d` | Keep price-multiplier sliders inside their grid cell | ✅ `PriceMultiplierField.tsx` adds explicit width/overflow constraints. |
| `a298a8d` | Render item & lote thumbnails through Drive proxy | ✅ `LoteResumenPage`, `EditItemDrawer`, `ProductoSpotlight`, `SubLoteDrawer` all route `driveFileId`s through `/api/serve-drive-image`. |

### Fotosíntesis — pricing (2)

| Commit | Claim | Verdict |
|---|---|---|
| `4bd2078` | Per-item x1–x4 price multiplier, drop precio potencial | ✅ New `PriceMultiplierField.tsx` (149 lines) + `utils/priceMultiplier.ts` (62 lines, pure) + `tests/priceMultiplier.test.ts` (11 tests). `LoteResumenPage` no longer references “precio potencial”. |
| `b9b2bc6` | Stop slider marks bleeding past rail + tune default multipliers | ✅ `PriceMultiplierField.tsx` tightens mark CSS; `LoteResumenPage` updates defaults. |

### Fotosíntesis — sublotes, publishing, SOT (5)

| Commit | Claim | Verdict |
|---|---|---|
| `13b1b58` | Add sub-lotes (sale-bundles) linked to parent lote | ✅ Complete domain slice: `convex/schema.ts` adds `subLotes` table (line 409); `convex/subLotes.ts` exposes 13 functions (queries, mutations, push action); `SubLotesPage.tsx` + `SubLoteCard.tsx` + `SubLoteDrawer.tsx` (742 lines combined); route `/lots/:loteId/sublotes` wired in `App.tsx:567`. Design doc at `docs/superpowers/specs/2026-05-25-fotosintesis-sublotes-design.md`. |
| `15b399c` | Sync SOT sheet schema with all current form fields | ✅ `api/_lib/fotosintesis-inventory-columns.{ts,js}` introduced; `api/admin-product-update.ts` rewritten (237 lines changed); `scripts/extend-fotosintesis-headers.mjs` (197 lines) to migrate the existing sheet; spec at `docs/specs/2026-05-25-fotosintesis-sheet-schema-sync.md`. |
| `702c09a` | Surface published lot items in customer catalog | ✅ `convex/products.ts` adds `publishedFotosintesisProducts` query; `useFotosintesisCatalog.ts` (new) feeds `useTreasure` so the browser merges Convex + Sheets sources. |
| `35336be` | Let closed lots be published from the resumen page | ✅ `LoteResumenPage.tsx` (175 lines changed) adds the publicar action for `cerrado` lots. |
| `b51ec94` | Keep published lotes reachable + sublotes claim their items | ✅ `convex/products.ts` extends the publish query to include `publicado`; `LoteResumenPage` reflects the new estado-aware actions. |

### Fotosíntesis — fixes (5)

| Commit | Claim | Verdict |
|---|---|---|
| `5edd322` | Reclaim lot serial when an empty lot is cancelled | ✅ `convex/sequences.ts:61` `reclaimIfTail` rolls the sequence back atomically only when the cancelled lot is the tail; `lots.cancel` (line 283) uses it. Covered by `tests/fotosintesis-sequences.test.ts` (10 tests). |
| `52f6cf4` | Stop +57 country code duplicating in provider phone input | ✅ Both `ProveedorNuevoDrawer.tsx:101` and `ClienteFinalForm.tsx:80` strip leading "57" before reformatting. New `tests/fotosintesis-phone.test.ts` (2 tests). |
| `b77ba76` | es-CO number formatting, full grammar-check + predictive suggestions | ✅ `NumberInputWithCalc.tsx` rewritten (180 lines) for es-CO locale; vocabulary expanded in `data/vocabularies.ts`. |

### Catalog & product detail (4)

| Commit | Claim | Verdict |
|---|---|---|
| `a5dc74a` | Show lotes & sublotes as grouped bundle cards | ✅ Largest single commit (1,886 +/907 −). `convex/products.ts` aggregates by `groupId`; `useFotosintesisCatalog.ts` maps to `TreasureItem` with `isLote` + `loteItems`; `GridCard.tsx` renders the bundle badge; `ProductDetailPage.tsx` renders `LotePriceBreakdown`; `types/index.ts:341` documents the schema. |
| `d3c3097` | Per-piece lote detail + sublotes tab script | ✅ `loteDetail.ts` (72 lines, pure) + `tests/loteDetail.test.ts` (9 tests). `scripts/create-sublotes-tab.mjs` (147 lines) provisions the SOT tab. |
| `cfc0b77` | Clear per-item + total price breakdown for lotes | ✅ New `LotePriceBreakdown.tsx` (310 lines) shows total + piece list; `PriceDisplay.tsx` updated to defer to it for lote products. |
| `2907827` | Remove average-price line from lote breakdown | ✅ `averagePerPiece` deleted from `LotePriceBreakdown.tsx`; no string “c/u en promedio” remains. |
| `bf1928c` | Lote spec weight sums all pieces, not just the first | ✅ `useFotosintesisCatalog.ts:201-217` sums every numeric `peso`, skips string (jewelry) values, falls back to the first piece when all are non-numeric. |

### Build / infra (1)

| Commit | Claim | Verdict |
|---|---|---|
| `57d353f` | Skip convex deploy on preview builds (no deploy key) | ✅ New `scripts/build-app.mjs` branches on `CONVEX_DEPLOY_KEY`; `package.json` `build:vercel` uses it; `vercel.json` `buildCommand` → `npm run build:vercel`. Safe (no shell injection — uses `spawnSync` with arg list). |

## Open work (uncommitted)

Working tree carries an in-progress slice that extends `EditItemDrawer` and the
item-draft pipeline:

```
M convex/lotItems.ts             (+15 lines)
M convex/schema.ts               (+4 lines)
M src/pages/admin/Fotosintesis/components/EditItemDrawer.tsx  (+277 / −0 area)
M src/pages/admin/Fotosintesis/utils/buildLotItemPayload.ts   (+159 lines)
?? tests/itemDrafts.test.ts      (new — 13 tests already passing)
```

These compile and the new test file already passes. They look like a coherent
follow-up (item-draft persistence) — not stray edits — but they are not part
of any of the 32 commits in this window. Decide whether to commit this slice
or stash it; leaving it uncommitted means today's deploys do not carry it.

## Risks / follow-ups noted while auditing

1. **API tsconfig still red.** `api/invitations.ts` has 2 type errors that
   pre-date this window (since 2026-04-13). They do not block Vercel builds
   (Vercel transpiles per function), but `npm run lint` is currently broken
   for the API project. Worth a separate cleanup commit.
2. **Bundle size.** Main chunk is 1,084 kB / 337 kB gzipped. Not introduced by
   these commits, but `a5dc74a` (which rewrote `MediaGallery` 859/-, `GridCard`
   335/-, `ProductDetailPage` 580/-) is a natural moment to consider
   `manualChunks` for the treasure surface.
3. **Two product-side hooks now feed the same surface.** `useTreasure` merges
   Convex (`useFotosintesisCatalog`) and Sheets data. Worth a smoke test that
   verifies a published lote shows up in the customer catalog end-to-end —
   `smoke:fotosintesis` already exists; consider extending it.

## What I ran

```
git log --since="2026-05-25" --until="2026-05-27" --no-merges      # 32 commits
git show <hash> --stat / diff <hash>                                # per-commit
npx tsc --noEmit                                                    # ✅
npx tsc --noEmit -p api/tsconfig.json                               # 2 stale errors
npx vitest run                                                      # 133 / 133 ✅
npx vite build --outDir=/tmp/tm-dist --emptyOutDir                  # ✅
```

— audited 2026-05-26
