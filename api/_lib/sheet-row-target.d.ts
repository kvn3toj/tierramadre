/** Ver `sheet-row-target.js` para el porqué (incidente del append, 2026-08-03). */
export declare function resolveRowTarget(
  colA: string[][],
  itemId: string | number,
): { foundRow: number; targetRow: number; willAppend: boolean };
