# W1–W3 sobre Fotosíntesis web + SOT v4 — Plan de implementación (Fase 1)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development
> (recomendado) o superpowers:executing-plans, task por task.
> **Handoff obligatorio:** lee `docs/superpowers/specs/2026-08-01-w1-w3-sot-v4-handoff.md`
> completo antes de la Task 0 — contiene decisiones tomadas, números de paridad y trampas.

**Goal:** el motor de precios del Modelo v2 vive en Convex con paridad probada; W1 (lote),
W2 (casillas) y W3 (movimientos) funcionan en Fotosíntesis web contra Convex-dev; todo evento
se espeja push-only al libro «SOT v4 · Espejo (PRUEBAS)».

**Tech stack:** React + Vite + TS, Convex (dev `flexible-wolverine-803`), vitest.

## Global Constraints

- Rama de trabajo: `feat/w1-w3-sot-v4` desde `main`. **PROHIBIDO**: Convex prod
  (`grand-hippopotamus-162`), el SOT v3 vivo, y las env vars de Vercel.
- Antes de CADA commit: `npm run test:unit` verde + `npm run lint` limpio (tsc de src y api).
- TDD estricto: test primero, verlo fallar, implementar, verde, commit. Un commit por task.
- La hoja solo por cabeceras nombradas (`columnMaps.ts`). Nunca índice posicional.
- Si cambias una proyección de query pública: anótalo en el commit — frontend y Convex se
  despliegan juntos.
- Contradicción entre plan/spec y código real → PARA y repórtala.

## Definition of Ready — global (verificar en Task 0; si falla una, PARA)

- [ ] Handoff leído completo; los 4 docs del vault accesibles (si el vault no está montado,
  pídelos — no improvises el modelo de memoria).
- [ ] `git status` limpio en main actualizado; rama `feat/w1-w3-sot-v4` creada.
- [ ] `npx convex dev` autenticado contra `flexible-wolverine-803` (ni una mutation antes de
  confirmar el deployment con `npx convex env list` o equivalente).
- [ ] Libro «SOT v4 · Espejo (PRUEBAS)» creado en Drive y compartido con la service account
  `tierra-madre-inventory@winged-scout-480001-a9` (pídele el ID al usuario si no existe).
- [ ] Suite base verde en main (`npm run test:unit`) — anota el número de tests de partida.

---

### Task 0 — Reconocimiento (solo lectura, sin commits)

**DoR:** el global de arriba.

- [ ] Leer `convex/_lib/lotMath.ts`, `precioEspecial.ts`, `columnMaps.ts`, `authz.ts` y el
  schema (`lots`, `lotItems`, `productInventory`, `sales`, `asesorMovements`, `sequences`).
- [ ] Leer `CapturaLotePage.tsx` (estructura de pasos y submit), `EscanearPage.tsx`,
  `VentaPage.tsx`, `MovimientosKardexPage.tsx` — solo mapear, no tocar.
- [ ] Del lado anima-bot (solo lectura): `src/cotizador/precios.ts` y sus tests — es la fuente
  del port de Fase A.
- [ ] Producir `docs/superpowers/specs/2026-08-01-task0-reconocimiento.md`: mapa de qué existe,
  divergencias contra el handoff, y las firmas exactas del motor a portar.

**DoD:** el doc de reconocimiento existe y no encontró bloqueante sin reportar.

---

## FASE A — El motor de precios en Convex (paridad pinneada)

**DoR:** Task 0 cerrada; números de paridad del handoff a mano; `precioEspecial.ts` entendido
(la regla de temporada existente se REUSA para «regla vigente con fecha», no se duplica).

### Task A1 — `convex/_lib/motorPrecios.ts` (puro) + tests de paridad

- [ ] Test primero (`tests/` o `convex/_lib/__tests__/`, según convención del repo — Task 0 lo
  dice): los números de paridad del handoff, cada uno como test con nombre del caso real:
  `costoFijoUnitario(33_651_815, 76) === 442_787` · lote 10 → objetivo $2.306.348 · lote 14 →
  $3.282.620 · equilibrio real gema K/0,90 y joya K/0,71 · remate dentro/fuera de ventana
  (2026-08-15 vs 2026-09-01) · divisor por categoría (gema 0,60 / joya 0,41) — y el candado:
  categoría ausente ⇒ **throw**, nunca un default.
- [ ] Implementar portando la aritmética de anima-bot `precios.ts` (calcularK, precioVenta,
  regla vigente). `costoFijoUnitarioActual(ctx)` = gastos fijos del período ÷ COUNT(lotes
  activos) — el divisor sale de la tabla, no de una constante (decisión D2). Los gastos fijos
  del período viven en una tabla `configPrecios` con vigencia por fecha (patrón «tasa por
  período» — un cambio de tasa nunca reprecia lo vendido).
- [ ] **DoD A1:** paridad byte a byte con los 7+ casos; función pura sin reloj (fecha por
  parámetro); tsc limpio; commit
  `feat(motor): motor de precios del Modelo v2 en Convex, paridad pinneada contra anima-bot`.

### Task A2 — Recálculo con traza

- [ ] Test primero: alta de lote → `costoFijoUnitario` cambia para TODO el inventario activo y
  queda un doc en `recalculos` {ts, evento, divisorUsado, unidadesActivas, valorAnterior,
  valorNuevo}; venta que desactiva el último ítem de un lote → ídem; consignación → NO recalcula.
- [ ] **DoD A2:** recálculo disparado por mutations de alta/venta, con traza; nada retroactivo
  sobre `VENDIDA`; commit.

**DoD FASE A:** motor + recálculo en dev, paridad verde, cero UI tocada.

---

## FASE B — W1: Cerebro Racional sobre CapturaLotePage

**DoR:** Fase A cerrada; estructura de pasos de CapturaLotePage mapeada en Task 0.

### Task B1 — Schema del lote W1

- [ ] Test primero (validators de mutation): `lots` gana `categoriaFiscal` (**obligatoria** en
  el path W1: gema | joya | mixta→se resuelve por casilla), bloque joya opcional (tipoJoya,
  mineral, gramaje, costoPorGramo, presupuestoJoyaCOP), `costosVariables[]` ({concepto, monto}
  — landed cost como documentos que ajustan, no campo editado), `abonoCOP`/`saldoCOP`.
  Campos aditivos y opcionales a nivel schema (no romper filas legacy); la OBLIGATORIEDAD vive
  en la mutation de W1.
- [ ] **DoD B1:** schema desplegado en dev, mutations validan, filas legacy intactas; commit.

### Task B2 — El wizard W1 en la página

- [ ] Test primero (component/integration según convención): el paso «Datos base» exige
  categoría fiscal como PRIMER campo y muestra bloques condicionales (gema/joya) según ella;
  el **preview del motor** (K, equilibrio real, objetivo, multiplicador informativo) aparece
  al completar costo+unidades+categoría, con las advertencias («el fijo pesa más que la
  pieza») — y marca «REMATE vigente» dentro de la ventana.
- [ ] Implementar como paso evolutivo de CapturaLotePage detrás de flag/ruta nueva
  (`/admin/fotosintesis/captura-v4` o flag — Task 0 decide cuál es más barato); la página
  actual queda intacta como default.
- [ ] **DoD B2:** flujo completo capturable en dev; preview correcto contra los números de
  paridad; commit.

### Task B3 — Guardar crea casillas + dispara efectos

- [ ] Test primero: guardar W1 → N casillas en `PENDIENTE_CLASIFICAR` (en `lotItems` extendido
  o tabla `casillas` — Task 0 recomienda; preferir extender `lotItems` con `estadoCasilla` +
  campos W2 para no duplicar el join), recálculo (A2) disparado, fila del lote encolada al
  espejo (Fase E stub: cola en tabla `espejoOutbox`).
- [ ] **DoD B3:** el ciclo W1 completo (capturar → casillas → recálculo → outbox) en verde;
  commit. **DoD FASE B:** demo en dev: un lote real de prueba capturado de punta a punta.

---

## FASE C — W2: casillas con completeness

**DoR:** Fase B cerrada.

### Task C1 — La casilla

- [ ] Test primero: casilla con prefill heredado del lote (editable), campos W2 (calidad gama
  completa, gradoRareza, rangoVentaEsperadoCOP, tipoJoya/gramaje si joya, ct, color, corte,
  renombre) y **costoUnitarioRealCOP capturado** (D6); al llenarlo, validación
  Σ casillas ≈ costoTotal del lote con tolerancia — diferencia se MUESTRA como aviso
  persistente en el lote, jamás se ajusta sola.
- [ ] `categoriaFiscal` por casilla cuando el lote es `mixta`; el motor la exige (A1).
- [ ] **DoD C1:** commit.

### Task C2 — Completeness + gate de publicación + entrada QR

- [ ] Test primero: el lote muestra score X/N; `publicado` bloqueado con casillas incompletas
  salvo override explícito «publicar parcial» que queda registrado; `EscanearPage` con QR de
  ítem navega directo a su casilla.
- [ ] **DoD C2 / FASE C:** flujo de clasificación demo-able: capturar lote de 3 unidades,
  llenar 3 casillas vía QR, publicar; commit.

---

## FASE D — W3: movimientos unificados

**DoR:** Fase C cerrada. Releer §5.1–5.3 de la spec de wizards.

### Task D1 — El modelo de eventos

- [ ] Test primero: `movimientos` (evolución de `asesorMovements`, aditiva) acepta tipos
  VENTA | CONSIGNACION | DEVOLUCION | ASESOR con campos comunes + bloque venta condicional
  (cliente, **precioVentaReal obligatorio**, comisión%, pago comisiones a, forma de pago con
  sub-campos por método: crédito/transferencia/efectivo); efectos por tipo sobre
  `estadoCasilla`; `origenKardexEventId` enlaza graduación.
- [ ] Candado anti doble-venta: vender una modalidad (`modalidadGrupo`) bloquea a las hermanas.
- [ ] **DoD D1:** commit.

### Task D2 — UI unificada + graduación

- [ ] Test primero: `MovimientosKardexPage` ofrece el tipo VENTA (hoy no lo tiene) con la
  captura condicional; «Vender esta pieza» desde consignación precarga W3-VENTA con
  `origenKardexEventId`; registrar venta dispara recálculo (A2) + outbox.
- [ ] `VentaPage` existente queda intacta (riel viejo); la unificación de `sales.create` al
  ledger es decisión abierta #9 de la spec — NO refactorizarla aquí, solo espejar al mismo
  outbox.
- [ ] **DoD D2 / FASE D:** demo: consignación de 2 piezas → graduación de 1 a venta → estados
  y recálculo correctos; commit.

---

## FASE E — Espejo push-only «SOT v4 · Espejo (PRUEBAS)»

**DoR:** libro de PRUEBAS creado (DoR global); fases B–D encolando en `espejoOutbox`.

### Task E1 — El drenaje del outbox

- [ ] Test primero: consumidor del outbox hace **upsert idempotente por id** con cabeceras
  nombradas en pestañas Lotes / Casillas / Movimientos; retry no duplica; fallo de Sheets no
  revienta la mutation origen (queda en cola con intento++); pestaña Léeme escrita con la
  advertencia de espejo.
- [ ] **DoD E1:** capturar un lote en dev y ver su fila en el libro de PRUEBAS en <1 min; commit.

### Task E2 — Inventario de WRITABLE + deriva

- [ ] Producir `docs/superpowers/specs/2026-08-01-writable-inventory.md`: cada campo WRITABLE
  del pull actual (empezando por `mostrarComoLote`, `precioFinalCOP`) con su destino propuesto
  (control en UI y sale del pull | excepción temporal documentada). **Solo documento — no
  cambiar el pull de v3 en este plan.**
- [ ] Job de deriva (manual en dev): compara espejo vs Convex por checksum y reporta — test
  con una celda editada a mano.
- [ ] **DoD E2 / FASE E:** commit.

---

## Definition of Done — GLOBAL del plan

- [ ] Suite completa verde (≥ la línea base de Task 0 + los tests nuevos) y `npm run lint` limpio.
- [ ] Demo de punta a punta EN DEV: capturar lote (W1, con preview) → clasificar casillas por
  QR (W2) → consignar y graduar a venta (W3) → recálculo trazado → todas las filas visibles en
  el libro de PRUEBAS.
- [ ] Cero escrituras a: Convex prod, SOT v3, env vars de Vercel.
- [ ] `docs/superpowers/specs/2026-08-01-cierre-fase1.md`: qué se construyó, desviaciones del
  plan, decisiones nuevas tomadas (con quién), y la lista exacta de lo que la Fase 2 de SOT v4
  (migración + doble corrida) necesita de aquí.
- [ ] NADA mergeado a main ni desplegado a prod — eso es la Fase 3 de SOT v4, con sus propios
  gates (doble corrida + checklist de decisiones pendientes del handoff).

## Orden y paralelismo

A → B → C → D estrictamente secuencial (cada una consume la anterior). E1 puede empezar
apenas B3 exista (el outbox); E2 en cualquier momento tras Task 0. Si una fase revela un
bloqueante de diseño: PARA, escribe el hallazgo en el doc de cierre, y sigue con lo no
bloqueado — el criterio es el mismo del refactor de anima-bot: el plan cierra donde alcanza,
con el límite documentado.
